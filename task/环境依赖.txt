操作系统： Windows11、macOS或主流64位Linux。

主软件为Node.js20或更高的正式版本。程序使用Node.js自带的URL、文件系统和路径接口，没有额外npm依赖，可从PowerShell或系统终端执行。处理只读取本地输入，账号需要在解压目录中创建output的权限。

当前数据量适合普通多核CPU，不需要GPU。磁盘空间要能容纳输入包、完成程序和三份业务结果。入口应设置有限超时，以覆盖当前批次并避免异常无限等待。
